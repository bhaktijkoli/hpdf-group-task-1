- args:
    sql: CREATE TABLE "posts"("id" serial NOT NULL, "user_id" integer NOT NULL, "username"
      text NOT NULL, "text" integer NOT NULL, "image" integer NOT NULL, PRIMARY KEY
      ("id") )
  type: run_sql
- args:
    name: posts
  type: add_existing_table_or_view
